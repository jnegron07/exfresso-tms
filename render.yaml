services:
  - type: web
    name: exfresso-tms
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: SKIP_INSTALL_DEPS
        value: "false"
