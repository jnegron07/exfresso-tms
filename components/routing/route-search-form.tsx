"use client";
// This component is kept for backward compatibility but no longer used in the main routes page.
// The routes/search/page.tsx now renders its own inline form matching the Stitch design.

export function RouteSearchForm() {
  return null;
}
